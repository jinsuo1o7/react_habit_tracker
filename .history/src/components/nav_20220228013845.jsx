import React, { Component } from "react";

class Nav extends Component {
    state = {
        total: 0,
    };
    render() {
        const habits = [...this.props.habits];

        return <div>{this.}</div>;
    }
}

export default Nav;
